import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const GamesScreen = () => {
  return (
    <SafeAreaView>
      <Text>Games ...</Text>
    </SafeAreaView>
  );
};

export default GamesScreen;
